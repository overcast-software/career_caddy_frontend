# +title: Todo (Frontend)
#+startup: show2levels
#+NOTE: [2026-04-13] This file predates the Tailwind/HyperUI migration. Many items may be resolved. Verify before working on anything here.

* Inbox
* fixes
** resume [0/4]
*** TODO thinking for resume import — should also disable button so you can't upload twice
*** TODO when displaying resume, if no summary was generated, don't show the header tag
*** TODO [#A] the upload button should be disabled after upload started
*** TODO Notes at the subnav level is bad — but unclear where to put it
** questions and answers
*** TODO [#B] /questions/:id/answer has a "new" button but it means new question, not new answer
*** TODO when AI returns answer, add it to the list in questions/:id/answers (or redirect)
*** TODO saving a new manual answer stays in /new — should reroute to show       :bug:ui:
** summary
*** IDEA "create-new-summary" action in UI is missing — mostly AI generated but nice to create manually
** jobPosts
*** [?] [#B] should route job_post — maybe after save go to job-post/<id>
**** TODO find what it does now
*** index [0/4]
**** TODO "All posts" is always highlighted
**** TODO score went to jobPosts index and didn't actually start grabbing a score — kick off polling, redirect to jobPosts/id/scores
**** TODO Search at the top is ugly                                              :UI:
**** TODO cancel should go back to job-posts.show.job-applications
*** Edit [0/2]
**** TODO can't create company                                                   :bug:
**** TODO you can save without description                                       :bug:
**** TODO UI has old look                                                        :UI:
*** show [0/6]
**** TODO salary and location are being presented
**** TODO COPY button should say "Copy description"
**** TODO Edit should be next to Copy description — remove from subnav
**** summaries
***** TODO need ability to create summary from page
**** scrapes
***** TODO scrapes aren't being called
*** New [0/3]
**** TODO check "save and apply" — does it go to job-posts/id/job-applications/new?
**** TODO default "applied" should be "Applied"
**** TODO too many borders — remove the outer one
*** scrape [0/2]
**** TODO The font looks weird in the input                                      :ui:
**** TODO the button doesn't match                                               :ui:
*** questions [0/2]
**** TODO test "save and add another" — probably can remove or hide
**** TODO list row needs show in addition to answer; show still goes to questions/id/answers
*** applications
**** TODO resume dropdown needs to be close to apply
** jobApplications
*** TODO invalid dates should not be at the top — nulls sort first with -applied_at, needs backend NULLS LAST fix
**** TODO "+New Application" button breaks with warning about null/undefined model values
*** index [0/2]
**** [?] what does the column "applied" mean?
**** TODO deleting a record doesn't remove the row — just the data. Action buttons persist but are broken.
*** view [0/4]
**** TODO remove svg with the statuses or make it smaller
**** TODO job-application/id/questions is broken
**** TODO need to test destroy
**** TODO job post should go to "job-post/id/job-applications"
*** edit
**** TODO edit doesn't let you mess with statuses, only show does
**** TODO cancel button doesn't bring you back to job-application/show
**** TODO save should take you to job-application show
**** TODO dropdowns with search look strange on full-sized page — labels sit left instead of above
** cover-letters
*** edit [/]
**** TODO no option to delete — delete needs to go back to cover-letters/index
**** TODO favorite button not standard
*** new [0/3]
**** TODO don't need to select a resume for manual entry — place resume selection on same panel as "create cover letter"
**** TODO rename "create cover letter" to "AI craft cover letter"
**** TODO save button is not standard
** Companies
*** TODO answers tab can have a "new question" action
** Questions
*** TODO infinite scroll
*** TODO search
** Answers
*** TODO answers new button should go to questions and flash "select question to answer"
*** TODO search
** Summaries
*** TODO if there is no job for the summary, remove the job label
*** TODO search
*** TODO infinite scroll

* features
*** WAIT descriptions should have descriptions                                   :resume:
Make a join table so a description can describe a description. This is a feature for STAR on bullet points — same concept as Q&A.

* bugs
*** STRT [#B] edit resume is broken (edit.js)
Lowered priority — we don't actually need it. serializeAttribute application.js:22

* production readiness
#+NOTE: [2026-04-13] Counts below are from 2026-03-20 and may be stale. Re-run linters and tests to get current numbers before starting work.

** TODO [#A] Fix failing lint — JS
Run =npm run lint:js= to check current state.

** TODO [#A] Fix failing lint — HBS
Run =npm run lint:hbs= to check current state. Some may be auto-fixable with =--fix=.

** TODO [#A] Fix failing lint — CSS
Run =npm run lint:css= to check current state. Many may be auto-fixable.

** TODO [#A] Fix failing unit/integration tests
Run =npm --prefix frontend test= to get current count. Known issues:
- Stale route test files (routes renamed/removed)
- Component tests may need service stubs (store, session, currentUser, flashMessages, router)

** TODO [#B] Resolve ember-data deprecation warnings (breaks in ember-data 6.0)
Create explicit transform shims: =app/transforms/string.js=, =date.js=, =boolean.js=, =number.js=.

** TODO [#B] Verify GitHub Actions deploy secrets are set
Deploy workflow requires: DEPLOY_SSH_HOST, DEPLOY_SSH_USER, DEPLOY_SSH_KEY, DEPLOY_APP_DIR.

** TODO [#C] Confirm production environment config
- =config/environment.js=: verify API_HOST for production
- CSP headers, nginx.conf, Dockerfile

* projects
** PROJ /plan bootstrap
** PROJ chat
