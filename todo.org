# +title: Todo
#+startup: show2levels
* Inbox
** TODO need link to manual entry :UI:
manual entry what?  for sure summaries.
sidebar doesn't have link to manual job post entry.
** TODO career-data
find out why duplication occurs

** WAIT if you don't supply a resume when filling out job application it silently fails
where is a fix right now
refresh was the root cause but this will warn if no company is assigned
** NO [#B] job-post/scrape needs loading
Scrape is returning fast, but we need to poll it to get results
CLOSED: [2026-02-26 Thu 17:49]

[[file:~/Network/syncthing/Projects/career_caddy_frontend/README.md::career-caddy-frontend]]
** TODO when ai returns answer.  add it to the list in questions/:id/answers
maybe I meant redirect
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/models/question.js]]
This should happen anyway.  
** TODO [#A] the upload button should be disabled after upload successfull
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/serializers/application.js::const val = attrs\[serializedKey\];]]
** DONE /resumes/index favorite button needs work
CLOSED: [2026-02-26 Thu 17:51]
will not reflect favorite status
is outside of the card


** [?] [#B] should route job_post

[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/controllers/job-posts/new.js::}]]
** DONE don't let the token expire too early
CLOSED: [2026-02-26 Thu 17:55]
this is a
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/controllers/job-posts/new.js]]
* resume [0/4] [0/4]
** TODO The adding of a new resume is borked :resume:
Adding new resume is ok.  cloning is for customizing
I go to edit a resume when I should be cloning
** TODO descriptions should have descriptions :resume:
make a join table so that a description can describe a description
this is a feature for STAR on bullet points
** TODO thinking for resume import
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/controllers/job-posts/new.js::}]]

*** TODO should also disable button for upload so you can't upload twice
** TODO [#B] edit resume is broken  edit.js
lowered priority b/c we don't actually need it
serializeAttribute application.js:22
* features
** IDEA /job-post//:jp_id/questions
might want to make this a feature

[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/components/questions/form.hbs::as |company|]]
** IDEA when you create a scrape got to the scrape show page.  maybe add a poll
** IDEA "create-new-summary" action in UI is missing
mostly AI generated but would be nice to create one manually
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/controllers/job-posts/new.js]]
** TODO [#B] `/questions/:id/answer` has a "new" button but it doesn't mean new answer, it means new question.

* bugs
** TODO AI answer returning 500 from server
"ask ai" answer service is broken?
{
	"errors": [
		{
			"detail": "AI assist failed: 'list' object has no attribute 'question'"
		}
	]
}

[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/components/answers/form.js::import Component from '@glimmer/component';]]
** TODO if scrape fails the loading is still showing
on error is broken for spinnier.wrap()?
[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/controllers/questions/show.js::@service router;]]
*** after rewrite they are all breaking
** TODO job-application/:id/edit
:LOGBOOK:
CLOCK: [2026-02-13 Fri 18:42]--[2026-02-13 Fri 18:43] =>  0:01
:END:
if coming from another page resume doesn't populate

[[file:~/Network/syncthing/Projects/career_caddy_frontend/app/authenticators/jwt.js::const url = `${this.baseUrl}${this.authConfig.TOKEN_PATH}`;]]

*** TODO get a user story for this
** TODO saving a new manual answer stays in /new should reroute to show :bug:
** IDEA the refresh bug is that @route='model' should be @route='model.id' for /resume/edit//:id/
* production readiness
** Synopsis (as of 2026-03-20)
- 36/165 QUnit tests failing; CI blocks on push to main
- npm run lint fails: 197 CSS errors, 23 HBS errors, 3 JS errors
- Deploy pipeline (CI → Docker → SSH) is wired but GitHub deploy secrets unverified
- Several broken user flows documented in bugs section below
- ember-data deprecation warnings will become hard errors in ember-data 6.0

** TODO [#A] Fix failing lint — JS (3 errors, all unused vars)
- ~app/authenticators/jwt.js~ line 27: unused ~error~ variable
- ~app/authenticators/jwt.js~ line 41: unused ~refreshError~ variable
- one route file: unused ~query~ parameter
Run ~npm run lint:js~ to confirm locations

** TODO [#A] Fix failing lint — HBS (23 errors, 4 auto-fixable)
Run ~npm run lint:hbs --fix~ for the 4 auto-fixable ones first.
Manual fixes needed:
- ~no-yield-only~: component with only ~{{yield}}~ — add markup or delete file
- ~no-forbidden-elements~: two ~<script>~ tags in templates — move to app.js or remove
- ~no-action-on-submit-button~: 3 submit buttons with click actions — remove {{on "click"}} and use form's submit event instead
- ~no-builtin-form-components~: 2 uses of ~<Input>~ — replace with ~<input>~ + separate action
- ~require-input-label~: 3 unlabeled inputs — add ~<label for=...>~ or ~aria-label~
- ~no-inline-styles~: 2 elements — move to Tailwind classes
- ~no-log~: ~{{log}}~ in template — remove debug statement
- ~no-invalid-interactive~: click on non-interactive element — wrap in ~<button>~
- ~no-nested-interactive~: interactive inside ~<label>~ — restructure

** TODO [#A] Fix failing lint — CSS (197 errors, 144 auto-fixable)
Run ~npm run lint:fix~ first to auto-fix 144.
Manually address remaining 53 errors after that.

** TODO [#A] Fix 36 failing unit/integration tests
*** Stale route test files (routes were renamed/removed)
These test files reference modules that no longer exist — delete or update them:
- ~tests/unit/routes/applications/~ (edit, index, new, show) — route is now ~job-applications/~
- ~tests/unit/routes/questions-test.js~ — route is ~questions/index~, not ~questions~
- ~tests/unit/controllers/job-posts/show/job-application/new-test.js~ — controller doesn't exist
*** Broken model test
- ~tests/unit/models/questions-test.js~ — model name is ~question~ (singular), test file uses ~questions~; fix the ~moduleForModel~ call

*** Integration component tests (28 failing)
All component render tests fail — likely because ~store~ or auth services aren't being injected in test setup.
- Audit one failing test (e.g. ~answers/form~) to confirm the root cause
- Add any missing ~this.owner.register(...)~ stubs for ~store~, ~session~, ~currentUser~, ~flashMessages~, ~router~ services
- Once root cause is confirmed, apply fix pattern across all 28 component test files

** TODO [#B] Resolve ember-data deprecation warnings (breaks in ember-data 6.0)
Create explicit transform shims so ember-data stops auto-registering them:
- ~app/transforms/string.js~ → ~export { StringTransform as default } from '@ember-data/serializer/transform';~
- ~app/transforms/date.js~ → ~export { DateTransform as default } from '@ember-data/serializer/transform';~
- ~app/transforms/boolean.js~ → ~export { BooleanTransform as default } from '@ember-data/serializer/transform';~
- ~app/transforms/number.js~ → same pattern for NumberTransform
Update ~app/deprecation-workflow.js~ to un-silence these once the shims are in place.

** TODO [#B] Verify GitHub Actions deploy secrets are set
The deploy workflow requires these secrets set in the repo's GitHub environment named ~production~:
- ~DEPLOY_SSH_HOST~
- ~DEPLOY_SSH_USER~
- ~DEPLOY_SSH_KEY~
- ~DEPLOY_APP_DIR~
- Optional: ~DEPLOY_SSH_PORT~, ~GHCR_PULL_USER~, ~GHCR_PULL_TOKEN~
The ~build-and-push~ job uses ~GITHUB_TOKEN~ automatically — no action needed there.
Verify a manual ~workflow_dispatch~ run completes without the "secret is not set" early exit.

** TODO [#C] Confirm production environment config
- ~config/environment.js~: verify ~API_HOST~ for production points to ~https://api.careercaddy.online~
- CSP headers in environment.js — confirm they cover all external assets loaded in prod (fonts, CDN, etc.)
- ~nginx.conf~: confirm gzip, cache headers, and SPA fallback (~try_files $uri /index.html~) are all present
- Dockerfile: confirm static assets are built with ~npm run build~ (not ~start~) and served via nginx

* projects
** TODO chat
** TODO UI revampt
* PROJ UI revamp
** Phase 1: Analysis & Design System
*** TODO Document target site structure and components
- [ ] Screenshot all key pages from jobops.dakheera47.com
- [ ] Document color palette, typography, spacing system
- [ ] List all UI components needed (buttons, cards, forms, etc.)
- [ ] Map out navigation structure and routing
- [ ] Identify reusable component patterns

*** DONE Create design tokens/variables
- [X] Define CSS custom properties for colors
- [X] Define spacing scale
- [X] Define typography scale
- [X] Define breakpoints for responsive design

** Phase 2: Component Library Foundation
*** TODO Build base UI components
- [X] Button component with variants (using Materialize)
- [X] Added Materialize CSS framework
- [X] Added Material Icons
- [ ] Card component
- [ ] Form input components
- [ ] Navigation components
- [ ] Layout components (containers, grids)

*** DONE Update application styles
- [X] Create new base stylesheet
- [X] Add CSS reset/normalize
- [X] Implement design tokens
- [X] Integrated Materialize CSS
- [X] Created dark theme overrides for Materialize

** Phase 3: Page-by-Page Migration
*** TODO Identify page migration order
- [ ] List all current routes
- [ ] Prioritize by user flow importance
- [ ] Plan data/state management changes if needed

*** TODO Migrate core pages (in order)
- [ ] Index/dashboard page
- [ ] Job applications list/index
- [ ] Job application detail/show
- [ ] Questions list/index
- [ ] Question detail with answers
- [ ] Resume management pages
- [ ] Cover letter pages

*** TODO Migrate secondary pages
- [ ] Settings/admin pages
- [ ] Setup/onboarding flow
- [ ] About page

** Phase 4: Testing & Polish
*** TODO Cross-browser testing
*** TODO Responsive design testing
*** TODO Accessibility audit
*** TODO Performance optimization
*** TODO User acceptance testing

** Phase 5: Deployment
*** TODO Staging deployment
*** TODO Production deployment
*** TODO Monitor for issues

** Notes
- Need actual screenshots/details from jobops.dakheera47.com to proceed
- Consider using a feature flag to toggle between old/new UI during development
- May need to update Ember dependencies if using newer component patterns
