# compiled output
/dist/
/declarations/

# dependencies
/node_modules/

# misc
/.env*
/.pnp*
/.eslintcache
/coverage/
/npm-debug.log*
/testem.log
/yarn-error.log

# broccoli-debug
/DEBUG/
.aider*
.tool-versions
notes.org
*.sh
# ...but the prod nginx image's entrypoint scripts must ship (Dockerfile COPYs them).
!docker-entrypoint.d/
!docker-entrypoint.d/*.sh
# ...and so must the checked-in dev/verification scripts (npm run verify:*).
!scripts/
!scripts/*.sh
# CI/CD needs package-lock.json

# Docker overrides
docker-compose.override.yml

# Local Caddy configs
infra/caddy/*.local.caddy
public/assets/*

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
