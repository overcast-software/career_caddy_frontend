# compiled output
/dist/
/declarations/

# dependencies
/node_modules/

# misc
/.env*
/.pnp*
/.eslintcache
/coverage/
/npm-debug.log*
/testem.log
/yarn-error.log

# broccoli-debug
/DEBUG/
.aider*
.tool-versions
notes.org
*.sh
# CI/CD needs package-lock.json

# Docker overrides
docker-compose.override.yml

# Local Caddy configs
infra/caddy/*.local.caddy
public/assets/*
# Extension build artifacts — kept local so prod deploy doesn't serve them
# at careercaddy.online/extensions/* (Distribution policy: AMO-signed only).
public/extensions/*.zip

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
