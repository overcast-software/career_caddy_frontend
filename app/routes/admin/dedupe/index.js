import Route from '@ember/routing/route';

// Stub landing for /admin/dedupe. No model; the template explains how
// to start a comparison. Phase D will replace this with the suspected-
// duplicates queue (top N candidates surfaced for staff review).
export default class AdminDedupeIndexRoute extends Route {}
