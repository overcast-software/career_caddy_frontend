{{page-title "Import"}}
<RouteLayout @flashMessages={{this.flashMessages}}>
  <:subnav>
    ingestion
  </:subnav>
  <:main>
    <UploadForm @onUpload={{this.ingestResume}} @disabled={{this.uploadDisabled}} />
  </:main>
</RouteLayout>
