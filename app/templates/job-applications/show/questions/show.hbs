<div class="space-y-4">
  <div class="flex items-center justify-between">
    <LinkTo
      @route="job-applications.show.questions"
      class="text-sm text-gray-500 hover:text-gray-700 inline-flex items-center gap-1"
    >&larr; Questions</LinkTo>
    <LinkTo
      @route="job-applications.show.questions.show.answers.new"
      class="btn small"
    >New Answer</LinkTo>
  </div>

  <Questions::Item @question={{this.model}} />

  {{outlet}}

  {{#if this.showAnswersList}}
    <Answers::DetailList
      @answers={{this.model.answers}}
      @editRoute="job-applications.show.questions.show.answers.edit"
      @showQuestion={{false}}
    />
  {{/if}}
</div>