{{page-title "Edit Question"}}
<RouteLayout @flashMessages={{this.flashMessages}}>
  <:subnav>
    <LinkTo class="btn" @route="questions.show" @model={{this.model}}>View</LinkTo>
  </:subnav>
  <:main>
    <Questions::Form @question={{this.model}} />
  </:main>
</RouteLayout>
