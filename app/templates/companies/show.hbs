<RouteLayout @flashMessages={{this.flashMessages}}>
  <:subnav>
    {{this.model.name}}
    <LinkTo
      @route="companies.edit"
      @model={{this.model}}
      class="btn"
    >edit</LinkTo>
  </:subnav>
  <:main>
    <Panel::Card @title={{this.model.name}}>
      <dl class="flex flex-wrap gap-y-2 text-sm">
        <LinkTo
          @route="companies.show.job-posts"
          @model={{this.model.id}}
          class="flex items-center gap-2 border-r border-gray-300 dark:border-gray-600 pr-4 mr-4 last:border-r-0 last:pr-0 last:mr-0"
        >
          <dt class="text-gray-500 dark:text-gray-400">Job Posts</dt>
          <dd
            class="font-semibold text-gray-900 dark:text-gray-100"
          >{{this.model.jobPostsCount}}</dd>
        </LinkTo>
        <LinkTo
          @route="companies.show.job-applications"
          @model={{this.model.id}}
          class="flex items-center gap-2 border-r border-gray-300 dark:border-gray-600 pr-4 mr-4 last:border-r-0 last:pr-0 last:mr-0"
        >
          <dt class="text-gray-500 dark:text-gray-400">Applications</dt>
          <dd
            class="font-semibold text-gray-900 dark:text-gray-100"
          >{{this.model.jobApplicationsCount}}</dd>
        </LinkTo>
        <LinkTo
          @route="companies.show.scrapes"
          @model={{this.model.id}}
          class="flex items-center gap-2 border-r border-gray-300 dark:border-gray-600 pr-4 mr-4 last:border-r-0 last:pr-0 last:mr-0"
        >
          <dt class="text-gray-500 dark:text-gray-400">Scrapes</dt>
          <dd
            class="font-semibold text-gray-900 dark:text-gray-100"
          >{{this.model.scrapesCount}}</dd>
        </LinkTo>
        <LinkTo
          @route="companies.show.questions"
          @model={{this.model.id}}
          class="flex items-center gap-2 border-r border-gray-300 dark:border-gray-600 pr-4 mr-4 last:border-r-0 last:pr-0 last:mr-0"
        >
          <dt class="text-gray-500 dark:text-gray-400">Questions</dt>
          <dd
            class="font-semibold text-gray-900 dark:text-gray-100"
          >{{this.model.questionsCount}}</dd>
        </LinkTo>
        <LinkTo
          @route="companies.show.scores"
          @model={{this.model.id}}
          class="flex items-center gap-2 border-r border-gray-300 dark:border-gray-600 pr-4 mr-4 last:border-r-0 last:pr-0 last:mr-0"
        >
          <dt class="text-gray-500 dark:text-gray-400">Scores</dt>
          <dd
            class="font-semibold text-gray-900 dark:text-gray-100"
          >{{this.model.scoresCount}}</dd>
        </LinkTo>
      </dl>
    </Panel::Card>

    <Panel::Card>
      <Companies::Tabs @company={{this.model}} />
      <div role="tabpanel" class="mt-4">
        {{outlet}}
      </div>
    </Panel::Card>
  </:main>
</RouteLayout>