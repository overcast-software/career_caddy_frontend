{{#if this.model.length}}
  <div class="overflow-x-auto rounded-lg border border-gray-200 bg-white">
    <table class="min-w-full text-sm">
      <thead class="bg-gray-50 border-b border-gray-200">
        <tr>
          <th class="px-4 py-3 text-left font-semibold text-gray-600">Job Post</th>
          <th
            class="px-4 py-3 text-left font-semibold text-gray-600"
          >Company</th>
          <th
            class="px-4 py-3 text-left font-semibold text-gray-600"
          >Status</th>
          <th
            class="px-4 py-3 text-left font-semibold text-gray-600"
          >Applied</th>
          <th
            class="px-4 py-3 text-right font-semibold text-gray-600"
          >Actions</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        {{#each this.model as |jobApplication|}}
          <JobApplications::Compact @jobApplication={{jobApplication}} />
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <p class="text-gray-500 text-sm py-6 text-center">No job applications for this
    company.</p>
{{/if}}