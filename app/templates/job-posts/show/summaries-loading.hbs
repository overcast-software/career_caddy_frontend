{{! Rendered into job-posts.show's outlet while the summaries route's
    model() promise is pending. Mirrors summaries.hbs (new-summary
    panel + Summaries::List table). }}
<NewActionPanelSkeleton @withInstructions={{true}} />

<section class="mb-6">
  <div
    class="mb-2 h-4 w-24 animate-pulse rounded bg-gray-200 dark:bg-gray-700"
  ></div>
  <TableSkeleton @hasActionsCol={{true}} />
</section>