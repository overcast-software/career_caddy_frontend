<Menus::SubnavBar>
  <LinkTo @route="job-posts.edit" class="btn" @model={{this.model}}>Edit</LinkTo>
</Menus::SubnavBar>
<JobPosts::Item
  @showControls={{this.showControls}}
  @jobPost={{this.model}}
/>
{{outlet}}
