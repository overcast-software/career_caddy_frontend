<JobPosts::Controls
  @query={{this.query}}
  @compact={{this.compact}}
  @onFilterChange={{this.onFilterChange}}
  @onCompactToggle={{this.onCompactToggle}}
/>
<div class='container'>
  <JobPosts::List
    @jobPosts={{this.model}}
    @query={{this.query}}
    @compact={{this.compact}}
  />
</div>
