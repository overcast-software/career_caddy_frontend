<div class="profile text-center">
  <div class="text-lg font-semibold">{{@user.name}}</div>
  <div class="text-[var(--muted)]">{{@resume.title}}</div>
  <div class="text-sm">{{@user.email}}</div>
  <div class="text-sm">{{@user.phone}}</div>
</div>