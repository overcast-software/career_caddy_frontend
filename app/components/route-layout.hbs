{{#unless @noSubnav}}
  {{#in-element this.subnavPortal insertBefore=null}}
    {{#liquid-bind this.routeKey class="subnav-swap"}}
      <Menus::SubnavBar>
        <:left>
          <Breadcrumbs />
        </:left>
        <:center>
          {{yield to="subnav"}}
        </:center>
        <:right>
          {{#if this.spinner.isShowing}}
            <div class="spinner-status">
              <span class="spinner-dot"></span>
              {{#if this.spinner.label}}
                <span class="spinner-label">{{this.spinner.label}}</span>
              {{/if}}
            </div>
          {{/if}}
        </:right>
      </Menus::SubnavBar>
    {{/liquid-bind}}
  {{/in-element}}
{{/unless}}
<BarCart @flashMessages={{@flashMessages}} />
<div class="fairway {{@mainClass}}">
  {{yield to="main"}}
</div>
{{yield to="outlet"}}