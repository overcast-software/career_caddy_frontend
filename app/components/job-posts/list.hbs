{{#each this.jobPosts as |jobPost|}}
  {{#unless jobPost.isNew}}
    {{#if @compact}}
      <JobPosts::Compact @jobPost={{jobPost}} @showLoading={{@showLoading}} />
    {{else}}
      <JobPosts::Item @jobPost={{jobPost}} @showLoading={{@showLoading}} />
    {{/if}}
  {{/unless}}
{{/each}}
