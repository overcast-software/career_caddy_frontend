{{yield}}
<PowerSelect
  @options={{this.postByCompany}}
  @selected={{this.selectedJobPost}}
  @onChange={{this.updateJobPost}}
  @searchEnabled={{true}}
  @search={{this.searchJobPosts}}
  @placeholder="Search job posts by title or company…"
  as |jobPost|
>
  {{jobPost.title}}
</PowerSelect>
