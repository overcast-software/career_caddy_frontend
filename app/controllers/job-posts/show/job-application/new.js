import Controller from '@ember/controller';

import { service } from '@ember/service';
export default class JobPostsShowJobApplicationNewController extends Controller {
    @service store;
    // litterally nothing to go in here
}
